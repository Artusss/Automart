<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Automart.Views.AddAdPage">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="QuitToolBar" Order="Default" Text="Закрыть" Clicked="Quit_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Padding="10" Margin="10, 0">
            <Label x:Name="VINLabel" Text="VIN" FontSize="Large" TextColor="Black" />
            <Entry x:Name="VINEntry" />
            <Label x:Name="VINErrorLabel" Text="Не показывается в объявлении" FontSize="Small" />
            
            <Label Text="Введите VIN и мы по возможности заполним объявление автоматически" FontSize="Small"
                   TextColor="Black" HorizontalOptions="CenterAndExpand"
                   BackgroundColor="LightBlue" Padding="15" Margin="0, 15" />

            <Button Text="Далее" VerticalOptions="EndAndExpand" Margin="0, 15" Clicked="Continue_OnClicked" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>